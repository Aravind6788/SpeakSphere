<!DOCTYPE html>
<html lang="en">
<%- include('partials/head') %>
<body class="bg-gray-50">

  <%- include('partials/nav') %>
  
  <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <!-- Hero Section -->
    <div class="grid grid-cols-1 md:grid-cols-2 gap-12 items-center">
      <!-- Create Room Section -->
      <div class="flex flex-col items-center text-center">
        <div class="relative w-full max-w-md mb-8">
          <img 
            src="/images/createm.png" 
            alt="Create Room Illustration" 
            class="w-full h-auto"
          >
        </div>
        <h3 class="text-xl font-semibold mb-4">Create Your Room</h3>
        <p class="text-gray-600 mb-6">Start a new collaboration space and invite your team members to join.</p>
        <a href="/room" class="bg-black text-white px-8 py-3 rounded-full hover:bg-gray-800 transition-colors duration-200 flex items-center gap-2">
          <span>Create Room</span>
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-plus-circle"><circle cx="12" cy="12" r="10"/><path d="M8 12h8"/><path d="M12 8v8"/></svg>
        </a>
      </div>
  
      <!-- Join Room Section -->
      <div class="flex flex-col items-center text-center">
        <div class="relative w-full max-w-md mb-8">
          <img 
            src="/images/joinmm.png" 
            alt="Join Room Illustration" 
            class="w-full h-auto"
          >
        </div>
        <h3 class="text-xl font-semibold mb-4">Join Existing Room</h3>
        <p class="text-gray-600 mb-6">Enter a room code to join your team's collaboration space.</p>
        <a href="/join-meeting" class="bg-black text-white px-8 py-3 rounded-full hover:bg-gray-800 transition-colors duration-200 flex items-center gap-2">
          <span>Join room</span>
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-log-in"><path d="M15 3h4a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2h-4"/><polyline points="10 17 15 12 10 7"/><line x1="15" x2="3" y1="12" y2="12"/></svg>
        </a>
      </div>
    </div>
  
    <!-- Features Grid Section -->
      <!-- <%- include('partials/footer') %> -->

  <script>
    // Mobile menu functionality
    document.addEventListener('DOMContentLoaded', function() {
        // Initialize Lucide icons
        lucide.createIcons();

        // Mobile menu toggle
        const mobileMenuButton = document.querySelector('.mobile-menu-button');
        const mobileMenu = document.querySelector('.mobile-menu');

        if (mobileMenuButton && mobileMenu) {
            mobileMenuButton.addEventListener('click', function() {
                mobileMenu.classList.toggle('hidden');
            });
        }
    });

    feather.replace();
</script>
</body>
</html>
   